services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: airwise
      POSTGRES_PASSWORD: airwise
      POSTGRES_DB: airwise
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
