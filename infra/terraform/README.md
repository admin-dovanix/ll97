# Terraform Placeholder

This folder is reserved for later infrastructure automation.

Near-term targets:

- managed Postgres
- object storage
- app hosting for `apps/web`
- API hosting for `apps/api`
- worker runtime for `apps/workers`
